Version 1.5.0
-------------
* **Bug Fix:** Fixed incorrect timestamps resulting from `messageStart` being constant.
* **Tweak:** Improved look and readability of date stamps in log files.
* **Tweak:** No longer exits Node if the package fails to set up correctly.

Version 1.6.0
-------------
* **Feature:** Added log names, shown next to all messages from a specific instance of the
  package.
* **Tweak:** Updated some of the wording in the README, also added new documentation for
  the new feature.
* **Fixes:** Small code tweaks for efficiency.

Version 1.6.1
-------------
* **Tweak:** `useLogName` is no longer required when using `logName`. It should now only
  be used when the filename is wanted.
* **Tweak:** Logging of objects now starts on a new line.
* **Bug Fix:** Stopped [undefined] showing up where the log name should be.

Version 1.6.2
-------------
* **Tweak:** Changed `logger.warning()` to `logger.warn()`.

Version 1.6.3
-------------
* **Tweak:** Moved the log name in front of the log level.

Version 1.6.4
-------------
* **Tweak:** Stopped functions from being printed as `undefined`.